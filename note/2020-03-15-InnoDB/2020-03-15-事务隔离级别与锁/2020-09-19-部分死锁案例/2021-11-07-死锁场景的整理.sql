

1. 同一张表下的死锁
	事务内的SQL语句加锁次序不一致
	事务内的SQL语句加锁次序一致
	
2. 不同表下的死锁
	不同表相同记录行锁冲突.sql
	
3. 间隙锁下的死锁

4. 唯一键冲突导致insert语句死锁


5. 更新语句的执行计划使用了合并索引导致的死锁   

6. 根据二级索引的范围更新，需要访问到不满足条件的第一行为止    

7. 更新语句使用了二级索引+主键索引作为条件，数据库选错了索引导致死锁  

8. pt-osc导致的死锁

9. 并发更新二级索引的状态字段

