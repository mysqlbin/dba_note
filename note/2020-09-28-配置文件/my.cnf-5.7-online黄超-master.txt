# For advice on how to change settings please see
# http://dev.mysql.com/doc/refman/5.7/en/server-configuration-defaults.html
# *** DO NOT EDIT THIS FILE. It's a template which will be copied to the
# *** default location during install, and will be replaced if you
# *** upgrade to a newer version of MySQL.

[client]
default-character-set=utf8mb4
port =3306
socket=/data_volume/mysql/mysql.sock

[mysql]
default-character-set=utf8mb4

[mysqld]

# Remove leading # and set to the amount of RAM for the most important data
# cache in MySQL. Start at 70% of total RAM for dedicated server, else 10%.
# innodb_buffer_pool_size = 128M

# Remove leading # to turn on a very important data integrity option: logging
# changes to the binary log between backups.
# log_bin

# These are commonly set, remove the # and set as required.
datadir=/data_volume/mysql
port =3306
server_id=1
pid-file=/var/run/mysqld/mysqld.pid
socket=/data_volume/mysql/mysql.sock
user=mysql

##error-logs
log_error = /data_volume/mysql/mysql-error.log 
###binlog
binlog_format=row
binlog_rows_query_log_events=on
log-bin=/data_volume/mysql/mysql-bin
log-bin-index=/data_volume/mysql/mysql-bin.index
# binlog日志设置保存时间7天
expire_logs_days = 7
# binlog二进制日志默认大小
max_binlog_size = 1G


### innodb
#innodb使用内存
innodb_buffer_pool_size = 10G
innodb_buffer_pool_instances = 2
innodb_log_buffer_size = 128M
innodb_log_file_size = 256M
innodb_file_per_table =ON
innodb_open_files=2M
innodb_thread_concurrency=0
innodb_print_all_deadlocks=1
#开启innodb的一表一个文件的设置
innodb_file_per_table = 1
#定时任务机制
event_scheduler = on

### myisam
key_buffer_size = 32M
sort_buffer_size=16M
query_cache_size=128M
query_cache_type=1
bulk_insert_buffer_size=16M
read_rnd_buffer_size=16M


#连接数
max_connections = 3000

character-set-client-handshake = FALSE
character-set-server = utf8mb4
init_connect='SET NAMES utf8mb4'
# collation-server=utf8mb4_unicode_ci


#开启log-bin日志
log-bin-trust-function-creators=1
#开启事件调度器
event_scheduler=1

#忽略大小写
lower_case_table_names = 1
#即使主库又是从库的要配置
log-slave-updates=1
#连接池超时配置
wait_timeout=31536000
interactive_timeout=31536000
lower_case_table_names=1
#禁止域名解析
skip-name-resolve



#慢查询开启
slow_query_log=1   
#慢查询时间为2s
long_query_time=2  
#存放位置 slow.log需要自己建 用户和组为mysql
slow_query_log_file =/data_volume/mysql/slow.log
#未使用索引的查询也被记录到慢查询日志中
log_queries_not_using_indexes
#将慢管理语句记入慢查询日志。
#log_slow_admin_statements
# Remove leading # to set options mainly useful for reporting servers.
# The server defaults are faster for transactions and fast SELECTs.
# Adjust sizes as needed, experiment to find the optimal values.
# join_buffer_size = 128M
# sort_buffer_size = 2M
# read_rnd_buffer_size = 2M 

sql_mode=NO_ENGINE_SUBSTITUTION,STRICT_TRANS_TABLES 



