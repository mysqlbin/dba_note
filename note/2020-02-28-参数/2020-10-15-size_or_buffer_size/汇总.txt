
tmp_table_size：
不是所有的临时表都是内存表, tmp_table_size这个参数限制了内存临时表的大小, 默认是16M; 
如果临时表大小超过了 tmp_table_size, 那么内存表会转成磁盘临时表。


sort_buffer_size：
就是 MySQL 为排序开辟的线程内存的大小。如果要排序的数据量小于 sort_buffer_size，排序就在内存中完成。
但如果排序数据量太大，内存放不下，则不得不利用磁盘临时文件辅助排序。